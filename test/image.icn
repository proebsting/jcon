record point(x,y)

procedure main()
   dump()
   dump(47)
   dump(3.14159)
   dump('712')
   dump("asparagus")
   dump(main)
   dump(write)
   dump(point)

#%#% the following don't work because the images are incomplete
#%#%   dump(point(1,2))
#%#%   dump(create 1 to 5)
end

procedure dump(x)
   write(type(x), " : ", image(x))
   return
end
