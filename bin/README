jcon/bin -- Jcon "binary" directory

This directory holds everything needed to compile programs using Jcon.
After building, all other directories can be discarded -- although you
may wish to keep the "html" directory, which contains documentation.

For more information about Jcon, see http://www.cs.arizona.edu/icon/jcon/.

Files in here are:

jcont		script for building programs with Jcon

jtran		actual translator and code generator program
jlink		helper program used in linking phase

jjtran		translator as built by Jcon
jjlink		linker as built by Jcon

rts.zip		run-time library (contains Java class files)

keysyms.icn	revised $include file containing keycode definitions
