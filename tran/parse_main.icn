link parse
link add_defaults

procedure main(L) 
    tok := create yylex(&input)
    tok_rec := @tok

    every d := program() do {
	    if not find("nodefaults", !L) then {
		add_defaults(d)
	    }
	    if find("dump", !L) then {
		s := verbose(d, "AST", "")
		write(s)
	    }
    }
end
