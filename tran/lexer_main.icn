link lexer

procedure main()
    tok := create yylex(&input)
    while T := @tok do {
	writes(\T.sym)
	writes(": ")
	writes(image(\T.str))
	writes(" file:", \T.coord.file, " line:", \T.coord.line, " column:", \T.coord.column)
	write()
    }
end
